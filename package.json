{
  "name": "supabase-branching-test",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "supabase": "^2.41.2"
  }
}
